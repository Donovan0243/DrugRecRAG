# SFT 微调所需依赖（在 VM GPU 上安装）

# 核心依赖
torch>=2.0.0
transformers>=4.35.0
peft>=0.6.0
datasets>=2.14.0
accelerate>=0.24.0

# 量化支持（QLoRA）
bitsandbytes>=0.41.0

# 训练监控
tensorboard>=2.14.0

# 可选：用于数据可视化
pandas>=2.0.0
matplotlib>=3.7.0

# 注意：
# 1. 在 VM 上安装时，确保使用 CUDA 版本的 PyTorch：
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# 2. bitsandbytes 需要 CUDA 11.8+ 或 12.1+
# 3. 如果使用 A100/H100，建议使用 bfloat16（--bf16 标志）

